// ignore: constant_identifier_names
// klotter runs denser than klator: the expression and result share the screen
// with a plot, so the type is smaller. Expression row height is derived from
// this ((FONTSIZE * 1.5) + 20 in main.dart), so it tightens with the font.
const double FONTSIZE = 20;
// ignore: constant_identifier_names
const String FONTFAMILY = 'OpenSans';
// ignore: constant_identifier_names
const int PRECISION = 6;
// ignore: constant_identifier_names
const double FRACTIONOFFSET = 0;
